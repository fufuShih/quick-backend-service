{
  "name": "quick-service",
  "version": "1.0.0",
  "description": "A package providing BaaS-like services",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm run --filter=\"./packages/*\" build",
    "cli": "pnpm --filter cli start",
    "dev": "pnpm run --parallel --filter=\"./packages/*\" dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.2.0",
    "typescript": "^5.5.4"
  },
  "workspaces": [
    "packages/**",
    "examples/**"
  ]
}
